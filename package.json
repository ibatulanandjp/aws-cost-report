{
  "name": "aws-cost-report",
  "version": "0.1.0",
  "bin": {
    "aws-cost-report": "bin/aws-cost-report.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "install-deps": "cd app && npm install && tsc && cd .. && npm install && tsc",
    "synth": "npm run install-deps && cdk synth",
    "diff": "npm run install-deps && cdk diff",
    "deploy": "npm run install-deps && cdk deploy"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "20.6.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "aws-cdk": "2.99.1",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.99.1",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}